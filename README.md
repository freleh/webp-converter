# Serve images in any format

Supported formats `jpeg|jpg|png|webp`

## Upload

Image upload, images are saved on `./images/`

## Serve

Image are read from `./images/` and converted into the requested image format.